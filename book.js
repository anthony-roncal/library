/* eslint-disable no-use-before-define */
/* eslint-disable no-console */
class Book {
    constructor(title, author, pages, isRead) {
        this.title = title;
        this.author = author;
        this.pages = pages;
        this.isRead = isRead;
    }
}
